[build]
  command = "npm run build"
  publish = "dist"

# Single-page application fallback so client-side routes (like /shop or
# /product/satin-bias-midi) survive a hard refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
